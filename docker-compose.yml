version: '3'
services:
  db:
    image: postgres:10.1-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - PGDATA=/pgdata
  app:
    build: .
    ports:
      - "3000:3000"
    links:
      - db
  