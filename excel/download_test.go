package excel

import (
	"testing"
)

func Test_download(t *testing.T) {
	// config.Init()
	// logger.Init()

	// file := fmt.Sprintf("http://localhost:%s/ping", config.Port())
	// err := download(file, "ping.html")
	// if err != nil {
	// 	assert.Fail(t, "Got an error in downloading the file")
	// }

	// os.Remove("ping.html")
}

func Test_DownloadLatest(t *testing.T) {
	// config.Init()
	// logger.Init()

	// err := DownloadLatest()
	// assert.Equal(t, nil, err)

	// os.Remove("./data/latestExcel.xlsx")
}
